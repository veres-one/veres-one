{
  "name": "veres-one-test",
  "version": "0.0.1-0",
  "description": "Veres One Tests",
  "private": true,
  "scripts": {
    "test": "redis-cli flushall && rm -f ./maintainer.jsonld &&  node --preserve-symlinks test.js test --framework mocha",
    "debug": "redis-cli flushall && rm -f ./maintainer.jsonld &&  node --preserve-symlinks test.js test --framework mocha --log-level debug",
    "watch": "grunt watch"
  },
  "dependencies": {
    "async": "^2.1.5",
    "bedrock-test": "^4.0.0",
    "did-veres-one": "github:veres-one/did-veres-one#v14-dingo-swap-cap-actions",
    "flex-docstore": "~0.1.2",
    "grunt": "^1.0.1",
    "grunt-cli": "^1.2.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-shell": "^2.1.0",
    "jsonld-signatures": "^5.1.0",
    "web-ledger-client": "^3.0.0"
  }
}
